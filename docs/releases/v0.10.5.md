# AuraScan v0.10.5

Release date: 2026-09-09.

AuraScan v0.10.5 is a recovery-bearing release because the Git source-acquisition
security boundary is shared with recovery. This candidate is not yet published.

## CodeWhale and DeepSeek trust boundaries

- Agent Instruction Guard discovers repository-local CodeWhale and legacy
  DeepSeek TOML config files. Clean files remain neutral baseline work.
  Credential-bearing user-global configs are excluded from project detection
  and reads. Supported top-level shell enablement requires HIGH review;
  malformed or unsupported TOML reports incomplete coverage. Settings are never
  executed, and a static match does not establish an affected installed version.
- Instruction references are checked against their containing project,
  including recursive and continued work. Escapes and credential-file targets
  are refused without reads; symlinks and ambiguous parent traversal remain
  manual-only. Findings preserve bounded line roles without source snippets.
  Continued discovery and cached analysis retain the project boundary without
  treating stale context as current authorization.
- Security audit checks captured CodeWhale/legacy package versions against nine
  verified upstream advisories. Unknown versions, ecosystem ambiguity and
  unverified backports remain explicit; exposure does not imply exploitation
  or a currently vulnerable AUR publication. Custom aliases and installations
  outside the captured package database are outside this version check.
- Source acquisition rejects option-shaped Git branch/tag fragments before Git
  runs. Inert runner-spy regressions cover decoded options and normal refs;
  package payloads and fixture commands are never executed by these tests.
- Updates `AGENTS.md`, `SKILL.md`, user and contributor documentation for the
  project/global configuration distinction and bounded TOML/import handling.

Sources verified September 9, 2026: [CodeWhale shell authority advisory](https://github.com/Hmbown/CodeWhale/security/advisories/GHSA-gx45-xrj5-g6c4),
[instruction-path advisory](https://github.com/Hmbown/CodeWhale/security/advisories/GHSA-62f5-cp2p-vq95),
[upstream security advisories](https://github.com/Hmbown/CodeWhale/security/advisories),
[configuration scope documentation](https://github.com/Hmbown/Codewhale/blob/main/docs/CONFIGURATION.md),
and [Git checkout option semantics](https://git-scm.com/docs/git-checkout).

The package-scanner rule version advances to `1.6.1`, and Instruction Guard
analysis evidence to `1.3`. The private continuation cursor advances to `1.1`
with legacy `1.0` reads. Scanner version stays `2.5.0`, package scan-input
version `2.0`, repository snapshot version `1.1`, Instruction Guard report/rule
version `1.0`, and Instruction Guard AI evidence version `1.1`. Application and
Arch/AUR package advance to v0.10.5. All AI modes remain opt-in; image build and
validation disable them explicitly.

## Recovery distribution and boundaries

This recovery-bearing release requires a fresh optional hybrid BIOS/UEFI
image as one indivisible set: `aurascan-recovery-0.10.5-x86_64.iso`, its
`.iso.sha256` sidecar, and sorted `.iso.packages.txt` manifest. The builder
rejects a GitHub release asset at or above 2 GiB. It requires fresh work and
output locations in a disposable Arch VM with externally bounded CPU, RAM,
and disk and no host disk or home shares.

Recovery remains optional. Locally built UKIs remain specific to the builder
kernel and disposable test keys; they are not universal download assets.
Every future release declares either `recovery-bearing` or `package-only`.
No previous release or tag is rewritten. A digest binds exact bytes, not
package safety, successful recovery, or the absence of every possible secret.

## Release validation

Recovery build candidate: `PENDING-VALIDATION`
ISO SHA-256: `PENDING-VALIDATION`

- Hybrid ISO, SeaBIOS readiness: `PENDING-VALIDATION`
- Hybrid ISO, OVMF UEFI readiness: `PENDING-VALIDATION`
- Local UKI, ordinary OVMF UEFI readiness: `PENDING-VALIDATION`
- Local UKI, disposable-key Secure Boot unsigned rejection and signed readiness: `PENDING-VALIDATION`
- Complete Python suite and Python 3.8/3.14 CI matrix: `PENDING-VALIDATION`
- Presenter, shell-syntax, unit, and package-metadata audits: `PENDING-VALIDATION`
- Clean Arch release-candidate package build: `PENDING-VALIDATION`
- Expanded-artifact privacy/path audit: `PENDING-VALIDATION`
- Strict sub-2-GiB ISO size gate: `PENDING-VALIDATION`
- Deterministic recovery scenario fixtures: `PENDING-VALIDATION`

The implementation work passed 3,102 local tests before release preparation.
That result does not establish the release candidate's source, package,
Python-version matrix, image, or boot gates. Publication remains blocked until
the required candidate gates complete and the exact image digest is pinned.
No v0.10.5 recovery image has yet been built; no image identity or successful
boot outcome is claimed in this candidate record.

Booted target-disk, Ethernet/DHCP, Wi-Fi, interactive offline diagnosis,
LUKS/Btrfs, ext4/LVM, interrupted-target repair, snapshot restore, initramfs
rollback, and bootloader refusal/reinstallation scenarios are `NOT RUN`.
AuraScan's target-disk and network workflow code is unchanged. Updated Arch
kernel and supporting packages in a fresh image would not establish their
live recovery behavior through service-readiness gates alone; no live
recovery outcome is claimed. Required deterministic scenarios remain separate
from objective no-NIC BIOS/UEFI service-readiness and UKI Secure Boot controls.
The bounded expanded-artifact privacy audit is also pending; nested initramfs
and EFI content remains opaque bytes and is not recursively validated by that
audit.
