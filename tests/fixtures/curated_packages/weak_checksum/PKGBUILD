# Maintainer: Fixture Maintainer <fixture@example.invalid>
pkgname=curated-weak-checksum
pkgver=1.0
pkgrel=1
pkgdesc="Defanged weak checksum fixture"
arch=("x86_64")
url="https://example.invalid/curated-weak-checksum"
license=("MIT")
source=("https://example.invalid/curated-weak-checksum-1.0.tar.gz")
md5sums=("0123456789abcdef0123456789abcdef")

build() {
  echo "defanged build step"
}
