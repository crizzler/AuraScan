# Security test documentation

The scanner must detect a fetch-plus-execute chain, but this document does not
instruct the agent to run one. Tests use `example.invalid`, never real attacker
infrastructure, and must not read a real home directory.
